---
date: 2026-02-27
tags: [bfs, 탐색, 효율성]
title: 백준 1012번 - 유기농 배추
---
# 백준 1012번 - 유기농 배추
## 문제
차세대 영농인 한나는 강원도 고랭지에서 유기농 배추를 재배하기로 하였다. 농약을 쓰지 않고 배추를 재배하려면 배추를 해충으로부터 보호하는 것이 중요하기 때문에, 한나는 해충 방지에 효과적인 배추흰지렁이를 구입하기로 결심한다. 이 지렁이는 배추근처에 서식하며 해충을 잡아 먹음으로써 배추를 보호한다. 특히, 어떤 배추에 배추흰지렁이가 한 마리라도 살고 있으면 이 지렁이는 인접한 다른 배추로 이동할 수 있어, 그 배추들 역시 해충으로부터 보호받을 수 있다. 한 배추의 상하좌우 네 방향에 다른 배추가 위치한 경우에 서로 인접해있는 것이다.

한나가 배추를 재배하는 땅은 고르지 못해서 배추를 군데군데 심어 놓았다. 배추들이 모여있는 곳에는 배추흰지렁이가 한 마리만 있으면 되므로 서로 인접해있는 배추들이 몇 군데에 퍼져있는지 조사하면 총 몇 마리의 지렁이가 필요한지 알 수 있다. 예를 들어 배추밭이 아래와 같이 구성되어 있으면 최소 5마리의 배추흰지렁이가 필요하다. 0은 배추가 심어져 있지 않은 땅이고, 1은 배추가 심어져 있는 땅을 나타낸다.
## 내가 푼 아이디어
배추밭을 2차원 리스트로 표현하고, 배추가 심어져 있는 곳을 1로 표시한다. 그리고 배추가 심어져 있는 곳에서 bfs를 사용하여 인접한 배추들을 탐색하면서 인접한 배추는 모두 없는 것으로 처리한다.

원래 greedy 방식으로 탐색하려고 했는데, 제대로 동작하지 않아 LLM과 상담하던 중 bfs를 사용하면 문제를 해결할 수 있다는 것을 알게 되었음.
```python
from collections import deque
t = int(input())
for _ in range(t):
    M, N, K = map(int, input().split())
    farm = [[0] * M for _ in range(N)]
    for _ in range(K):
        x, y = map(int, input().split())
        farm[y][x] = 1
    # 입력 끝
    # 디버깅용 배추 밭 출력
    # for i in range(N):
    #     for j in range(M):
    #         print(farm[i][j], end=' ')
    #     print()

    bug = 0
    for i in range(N):
        for j in range(M):
            # 각 칸을 순회하면서 배추가 심어져 있는 곳을 찾는다.
            if farm[i][j]:
                bug += 1
                # 배추가 숨어져있는 곳을 찾으면 그 배추를 기준으로 bfs를 사용하여 인접한 배추들을 탐색한다.
                farm[i][j] = 0
                queue = deque()
                queue.append((j, i))
                while queue:
                    x, y = queue.popleft()
                    for d in [[1,0],[0,-1],[-1,0],[0,1]]:
                        dy, dx = y + d[1], x + d[0]
                        if 0 <= dx < M and 0 <= dy < N:
                            if farm[dy][dx]:
                                # 인접한 배추가 있으면 그 배추도 없는 것으로 처리하고, 그 곳도 탐색 영역에 추가하기 위해 큐에 추가한다.
                                farm[dy][dx] = 0
                                queue.append((dx, dy))
    print(bug)
```
# 정리
1. 원래 greedy 방식으로 탐색하려고 했지만, 제대로 동작하지 않아 bfs를 사용하여 문제를 해결하였다.
2. bfs를 사용하여 인접한 배추들을 탐색하면서 인접한 배추는 모두 없는 것으로 처리하였다. 이때 처음에는 queue를 pop 할 때 farm의 배추를 삭제했는데, 그렇게 하면 큐에 추가된 배추가 삭제되어서 탐색이 제대로 이루어지지 않았다. 그래서 큐에 추가할 때 farm의 배추를 삭제하도록 수정하였다.

# 아쉬운점
1. AI 사용해서 힌트를 얻어낸 점.
