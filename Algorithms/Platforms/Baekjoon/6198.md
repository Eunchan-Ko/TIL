---
date: 2026-02-26
tags: [stack, 전체탐색]
title: 백준 6198번 - 옥상정원

---
# 백준 6198번 - 옥상정원
## 문제
[6198번 - 옥상정원](https://www.acmicpc.net/problem/6198)
관리인들은 오른쪽, 아래만 볼 수 있다.
각 관리인들이 벤치마킹이 가능한 빌딩의 수의 합을 출력한다.

## 내가 푼 아이디어
1. 전체 탐색을 돌려서 각 관리인이 볼 수 있는 옥상의 수를 트래킹
2. 스택의 길이가 관리자가 볼 수 있는 건물의 수인 것에 착안해 stack을 사용


```python
import sys
sys.stdin = open("input/6198.txt")

N = int(input())
buildings = []
for _ in range(N):
    buildings.append(int(input()))
stack = []
view = 0
# 전체탐색
# for i in range(N-1):
#     j = i + 1
#     while buildings[j] < buildings[i]:
#         view += 1
#         j += 1
#         if j == N:
#             break
# 전체탐색 끝.

# 스택 사용
# for building in buildings:
#     # 스택이 비어있으면 일단 추가
#     if not stack:
#         stack.append(building)
#         continue
#     # [10, 3, 7, 4, 12, 2]
#     if building >= stack[-1]:  # 현재 값이 과거 가장 최근 값보다 클 때 -> 더이상 옆을 보지 못함
#         pass
#         # building = 7일때 실행.
#         # til stack[-1] < building이라면, stack.pop이 실행될 때마다 view 할 수 있는 
#         can_view = 0
#         while stack and stack[-1] < building:
#             view += can_view
#             stack.pop()
#             can_view += 1
#         if stack and stack[-1] > building:
#             view += can_view
#         stack.append(building)
#     else:  # 현재 값이 스택 내부의 가장 최근 값보다 작을 때.
#         stack.append(building)
#
# # 반복문이 끝났는데, 스택에 남아있을 때. 구현 실패
# if stack:
#     can_view = 1
#     building = stack.pop()
#     while stack and stack[-1] > building:
#         view += can_view
#         building = stack.pop()
#         can_view += 1
#     if stack and stack[-1] > building:
#         view += can_view
# print(view)

# 스택 사용. 제미니이 추천 ver.
for building in buildings:
    while True:
        if stack and stack[-1] <= building:
            stack.pop()
        else:
            break
    view += len(stack)
    stack.append(building)
print(view)
```